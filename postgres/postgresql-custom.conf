wal_level = replica
archive_mode = on
archive_command = 'python3 /backup/b2_upload.py /var/lib/postgresql/data/pg_wal/ "wal/${file_name}" 2>/dev/null || true'
archive_timeout = 60
max_wal_size = 2GB
wal_keep_size = 1GB
listen_addresses = '*'
